# Variables de entorno para producción
# Renombra este archivo a .env y completa los valores

# Base de datos (requerido para producción)
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/nexustech

# Puerto (Vercel lo configura automáticamente)
PORT=3000

# Entorno
NODE_ENV=production